name: PR Review (SwiftLint + AI)

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    runs-on: macos-latest

    steps:
      # Checkout repo
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ------------------------
      # 1. SwiftLint + Reviewdog
      # ------------------------
      - name: Install SwiftLint
        run: brew install swiftlint

      - name: Run SwiftLint with reviewdog
        uses: reviewdog/action-reviewdog@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          reporter: github-pr-review
          level: warning
          fail_on_error: false
          filter_mode: diff_context
          tool_name: swiftlint
          run: |
            swiftlint lint --quiet

      # ------------------------
      # 2. AI PR Review
      # ------------------------
      - name: Generate PR Diff
        run: |
          git fetch origin ${{ github.base_ref }}
          git diff origin/${{ github.base_ref }}...HEAD > pr.diff

      - name: Run AI Review
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          review=$(curl -s https://api.openai.com/v1/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $OPENAI_API_KEY" \
            -d "{
              \"model\": \"gpt-4o-mini\",
              \"messages\": [
                {\"role\": \"system\", \"content\": \"You are a senior iOS developer reviewing pull requests. Provide concise, constructive comments about Swift, SwiftUI, MVVM, in-app purchases, unused variables, and best practices.\"},
                {\"role\": \"user\", \"content\": \"Review this PR diff:\n$(cat pr.diff)\"}
              ],
              \"max_tokens\": 500
            }" | jq -r '.choices[0].message.content')

          echo "AI Review: $review"

          # Post AI review as PR comment
          curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -X POST \
            -d "{\"body\": \"$review\"}" \
            "https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/comments"
